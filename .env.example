# AgentKit — Variables de entorno
# Copia este archivo como .env y llena tus datos:
#   cp .env.example .env
#
# Claude Code te ayuda a llenar estos valores durante el setup.
# NUNCA subas el archivo .env a GitHub.

# ── Gemini API (usado por el cerebro del agente) ─────────────────
# Obtener en: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# ── Proveedor de WhatsApp ─────────────────────────────
# Opciones: twilio
WHATSAPP_PROVIDER=twilio

# ── Twilio (si WHATSAPP_PROVIDER=twilio) ───────────────────
# Configurar en: https://twilio.com
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ── Servidor ───────────────────────────────────────
PORT=8000
ENVIRONMENT=development  # development | production

# ── Base de datos ──────────────────────────────────────
# Local (SQLite):
DATABASE_URL=sqlite+aiosqlite:///./agentkit.db
# Produccion (PostgreSQL en Railway):
# DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/dbname

# ── Integración de Google (Reservas) ────────────────────
# ID del spreadsheet de Google Sheets (lo que aparece en la URL)
GOOGLE_SHEET_ID=
# ID del calendario de Google Calendar (ej: tucorreo@gmail.com)
GOOGLE_CALENDAR_ID=

# Credenciales OAuth2 de Google en formato JSON (una sola línea, sin saltos).
# Para Railway: copia TODO el contenido de config/token.json aquí.
# Para desarrollo local: puedes dejar esto vacío y usar config/token.json + config/client_secret.json
# GOOGLE_CREDENTIALS_JSON={"token":"...","refresh_token":"...","token_uri":"...","client_id":"...","client_secret":"...","scopes":["..."],"universe_domain":"googleapis.com","account":"","expiry":"..."}

# ── Credenciales alternativas de Google (solo desarrollo local) ──────
# Estos archivos están en .gitignore y NO se suben a GitHub.
# Coloca los archivos en la carpeta config/ en tu máquina local.
# config/client_secret.json
# config/token.json